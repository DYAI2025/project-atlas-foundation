{
  "name": "project-atlas-foundation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "check": "npm test && node scripts/validate-current-repository.mjs"
  },
  "devDependencies": {
    "ajv": "8.20.0"
  }
}
